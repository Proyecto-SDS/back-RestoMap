# ============================================
# VARIABLES DE ENTORNO - RESTOMAP BACKEND
# ============================================
# Copia este archivo a .env y ajusta los valores

# ============================================
# BASE DE DATOS (PostgreSQL)
# ============================================
# Para desarrollo local
DB_HOST=localhost
DB_PORT=5432
DB_NAME=restomap
DB_USER=postgres
DB_PASSWORD=postgres

# Para Docker Compose (usar estos valores)
POSTGRES_DB=restomap
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Para GCP Cloud Run (Cloud SQL)
# DB_HOST=/cloudsql/PROJECT:REGION:INSTANCE

# ============================================
# APLICACION
# ============================================
# Entorno: development, production
ENV=development

# Puerto del servidor
PORT=5000

# ============================================
# SEGURIDAD
# ============================================
# Clave secreta para JWT (CAMBIAR EN PRODUCCION!)
JWT_SECRET_KEY=dev-secret-key-change-in-production-2025

# Origenes permitidos para CORS (separados por coma)
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ============================================
# LOGGING
# ============================================
# Nivel: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Formato de logs:
#   - text: Logs legibles con colores (recomendado para desarrollo local y Docker)
#   - json: Formato JSON estructurado (usado automaticamente en GCP)
#   - auto: Detecta automaticamente (JSON en Docker/GCP, text en local sin Docker)
# NOTA: En Docker local, usar 'text' para logs legibles
LOG_FORMAT=text

# Archivo de logs (opcional, no usar en GCP)
# LOG_FILE=logs/app.log

# ============================================
# FRONTEND (para docker-compose)
# ============================================
NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=tu_token_de_mapbox